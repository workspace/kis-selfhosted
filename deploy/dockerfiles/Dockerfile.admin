FROM python:3.13-slim

WORKDIR /app

RUN pip install uv

COPY deploy/admin/pyproject.toml ./
RUN uv sync --no-install-project

COPY deploy/admin/ .

ENV KIS_CONFIG_DIR=/app/kis-config

EXPOSE 8080
CMD ["uv", "run", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]
